根据端口号查找进程（netstat -lnp|grep 8081(端口号)）
根据进程查找端口号（netstat -naop|grep 3434(进程号)）
列出文件或者文件夹详细信息(stat 文件/文件夹/*(所有))
列出当前目录下所有文件（ls -l）(ls -l t*(列出以t开头的文件或者文件夹))
列出当前目录下所有文件(ls -a/-all)(可使用通配符)
列出当前目录下所有文件并且以逗号分开(ls -m)(可使用通配符)
列出当前目录下所有文件并且从大到小排序(ls -S)(可使用通配符)
列出当前目录下所有文件并且从旧到新排序(ls -ltr)(可使用通配符)
查看文件内容(cat 文件名)
cp 文件   文件夹路径（复制一个文件到另一个文件夹下中间尽量多敲空格）
pwd (显示当前目录)
显示当前登录用户(who)
显示当前操作用户(whoami)
现实最占资源的进程(top)